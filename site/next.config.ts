import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static-export compatible: this is a marketing landing page with no
  // server-side data. Deploying to Vercel / Cloudflare / Netlify all work.
};

export default nextConfig;
